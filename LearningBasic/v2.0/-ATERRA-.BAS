5 REM "ATERRAGEM"
CLS
SCREEN 12
10 PRINT TAB(30); "*** ATERRAGEM ***"
50 LOCATE 20, 15: PRINT "‹€‹‹€‹‹‹‹‹‹‹‹‹‹‹‹‹€"
70 LET F = 500
80 LET H = 500
90 LET S = 0
100 LET V = 0
110 IF H < 600 THEN LOCATE 20 - H / 30, 20
120 LOCATE 1, 1: PRINT "COMBUSTIVEL:"; F; " "; TAB(0); "ALTURA:"; H; " "; TAB(0); "VELOCIDADE"; S
140 IF INKEY$ = "R" AND F >= 5 THEN LET V = 1
150 IF V THEN LOCATE 21 - H / 30, 20: PRINT "V V"
160 IF F < 5 THEN LOCATE 1, 17: PRINT "* VAZIO *"
170 LET F = F - V
180 LET S = S - V + 1
190 LOCATE 20 - H / 30, 20: PRINT "    "; TAB(20); "   "
200 IF H < 30 THEN GOTO 230
210 LET H = H - S
220 GOTO 100
230 IF S < 4 THEN LOCATE 21, 10: PRINT "ATERRAGEM PERFEITA"
240 IF S < 8 AND S > 4 THEN LOCATE 21, 10: PRINT "VIOLENTO MAS TODOS SALVOS"
250 IF S >= 8 THEN LOCATE 21, 7: PRINT "A NAVE ESMAGOU-SE NO SOLO"
260 LOCATE 12, 1: INPUT "QUER JOGAR OUTRA VEZ"; A$
280 IF A$ = "S" THEN GOTO 5




